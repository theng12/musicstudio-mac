fastapi>=0.139
pydantic>=2.12
starlette>=1.0
uvicorn[standard]>=0.30
huggingface_hub>=1.0
sse-starlette>=2.1
python-multipart>=0.0.32
setproctitle>=1.3.7
# Host memory sizing for the idle-release policy's machine-aware default
# (memory_policy.default_mode). This is a base-server dependency, not a
# generation-stack one: default_mode() runs on every install regardless of
# whether the optional torch/transformers generation stack has been
# installed, and its ImportError fallback silently masks a missing psutil
# by defaulting to the roomy-machine mode — exactly the wrong direction on
# a small Mac. Previously only present as a transitive pin (via accelerate)
# in requirements-generation.lock.txt, so a base-only install never got it.
psutil>=7.0
